# 같은 반 학생 미표시 버그 수정 (서버 격리 적용)

- **ID**: 009
- **날짜**: 2026-03-11
- **유형**: 버그 수정

## 작업 요약
프로필 페이지에서 교사가 같은 반 아이를 볼 수 없던 버그 수정. `server_members` 테이블을 통한 서버 격리 필터링을 추가하여 현재 서버 소속 사용자의 아이만 조회하도록 수정. 원장도 자기 서버 소속 부모의 아이만 조회하도록 변경. 아이 데이터에 부모 닉네임도 함께 반환하도록 추가.

## 변경 파일 목록
- `src/app/page.note.profile/api.py`: `server_members` 기반 서버 격리 적용, `class_name.strip()` 정규화, `parent_nickname` 반환 추가
